{
  "dependencies": {
    "concurrently": "^9.0.1"
  },

  "scripts": {
    "start": "concurrently \"yarn install && cd server && yarn start\" \"cd client && yarn start\"",
    "install": "concurrently \"cd server && yarn install\" \"cd client && yarn install\""
  }
}
